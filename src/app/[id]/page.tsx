import {Flex, Text} from "@chakra-ui/react";

type Props = {
    params: {
        id: string;
    }
};

export default function Film({ params: { id } }: Props) {
    return (
        <Flex>

        </Flex>

    )
}